machine:
  environment:
    PERCY_BRANCH: gh-pages

dependencies:
  pre:
    - gem install percy-cli

test:
  override:
    - |
      if [ "$CIRCLE_BRANCH" = "gh-pages" ]; then
        export PERCY_BRANCH="master"
      fi

      percy snapshot --widths "375,1280" .
